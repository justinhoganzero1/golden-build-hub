import PageShell from "@/components/PageShell";
import { Heart } from "lucide-react";
const DiagnosticsPage = () => <PageShell title="Diagnostics" subtitle="Health diagnostics & tracking" icon={<Heart className="w-6 h-6" />} />;
export default DiagnosticsPage;
